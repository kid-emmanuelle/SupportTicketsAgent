# Development dependencies
-r requirements.txt

# Testing
pytest>=8.0.0

# Formatting / linting
ruff==0.14.13   # pinned to avoid breaking CI

# Optional but common
# black>=24.0.0
# pre-commit>=3.7.0

# Notebook support (only enable if you actually use notebooks)
# ipykernel>=6.29.0